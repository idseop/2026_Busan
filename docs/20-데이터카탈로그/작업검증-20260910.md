# 2026-09-10 작업·검증 상태

## 데이터 실사 완료

- 사용자가 분석기간을 **2018~2024년, 7년**으로 확정했다. 확장 수집 결과와 기간별 제공 한계는 [추가 수집 보고서](부산-확장수집-20260910.md)에 반영한다.
- 연령별 주민등록인구2018~2024년7개와 성·연령별1인세대2022~2024년3개, 총10개38,654행 전수 검사 통과. 별도 파서로 파일명·헤더연월·지역 수·SHA256을 재검산했다. 근거는 `data/interim/인구배경/independent-period-verification.json`이다. 2018~2021년1인세대 연령통계는 미제공으로 기록했다.
- 추가 수집 원본18개를 카탈로그에 등록한 뒤 하네스10개 항목을 재실행해 오류·경고 없이 통과했다. 과거경계·연령별구급 로그인 자료는 원본 미확보이며 확보완료로 계산하지 않았다.
- 교통사고 다발지역 전체 CSV2개는 Python 감사와 독립 JavaScript 파서의 전국·부산 행 수·결측·해시가 일치한다. 증거는 `data/interim/교통안전/independent-verification.json`이다. 관측연도 열이 없어 신고와 연도 결합은 아직 수행하지 않았다.
- 확장 보고서 별도 리뷰에서 `RPTP_NO`의 의미를 신고번호에서 공식 스키마의 **보고서번호**로 정정했다. 임의 사건 연결은 수행하지 않았다.

- 사용자 ZIP 두 개를 압축해제하고 2018~2024 CSV 전체를 검사했다. ZIP CRC 검사와 추출 파일 크기 대조를 통과했다.
- Node 전수 감사와 독립 Python/pandas 전수 검산에서 행 수·모든 열 결측·좌표 품질 집계가 일치했다. 독립 검산 근거: `data/interim/119접수감사/independent-verification.json`.
- 노인일자리 XLSX의 모든 사업 행과 추출 셀을 원본과 대조했다. 일반 openpyxl 모드는 원본 스타일 ID 오류가 있었지만 읽기전용 모드와 ZIP XML 전수 읽기가 일치했다. 원본을 수정하지 않았다.
- 부산 노인복지관 전체 CSV와 공식 구급연보 두 개 원본을 추가 수신했다. 연보의 실제 부산 연령·인력·차량 표를 렌더링해 대조했다.
- 결과·38열 설명·분석 가능 범위는 [전수 실사](119신고접수-전수실사.md), 출처·원본 경로는 [카탈로그](INDEX.md)에 기록했다.

## 현재 주제 정리

README·CLAUDE·주제 안내·협업 문서·분석 안내·관련 명령과 스킬을 신고 기반 예방·지원활동 주제로 교체했다. 관광 추천·폭염 등 이전 주제의 자료, 실험 원본, 산출물, 빈 폴더를 삭제했다. 보관본을 만들지 않았다. 현재 사용자 제공 119 ZIP·추출 CSV와 현재 주제 보완자료는 유지했다.

삭제 잔여: `output/codex-topic-summary-20260908/.git/objects/`는 사용자가 나중에 직접 삭제하기로 했다. 해당 경로의 삭제는 현재 에이전트 작업에서 제외한다. 이전 강제 삭제는 자동 실행 검토에서 차단됐으며 추가 삭제를 시도하지 않는다.

## 코드·환경 점검

- 자체 웹 지도 구현: 부산16구군 경계·7년·A/B/C 조건·신고유형 전체 및5종·인구/65세 이상 비중·지역 선택·전체연령 상세를 연결했다. 원시 신고를 제공하지 않고 web 폴더만127.0.0.1 로컬 서버로 확인했다.
- 웹 기본63상태와 실패주입4종(404·JSON오류·집계합불일치·경계명불일치)을 독립 검증했다. 유형 확장 후126개 필터 상태에서 웹의 시·구군·미기재·상세 값을 JSON과 대조해 일치를 확인했다. 인구 지표12상태에서 신고유형에 따른 인구값 변경이 없었다. 증거: `data/interim/웹독립검증/`, `data/interim/웹종별검증/`.
- 화면390/768/1440폭·키보드·확대/초기화·오류표시 확인. 웹↔JSON 검사와 앞선 원본 재집계 검사는 구분한다. 외부 배포·접근권한·제출용 파일 검수 완료를 의미하지 않는다.
- 사용자 지시에 따라 제출 서류 작성·PDF 편집·패키징은 분석·웹 완성 후로 미뤘다. 공식 제출요건만 유지하며 현재 작업은 데이터와 웹 구현이다.

- 전체 연령 인구1,309행은 별도 pandas 파서로7년 원본에서 전 셀·비중·지역 총계를 재계산해 일치를 확인했다. 신고 조건비교136행도7년 원본6,236,450행에 조건을 다시 적용해 A/B/C·제외량·건수순위·변동을 모두 대조했다. 증거: `data/interim/전체연령조건검증/independent-verification.json`. 이 검증은 앞선2024 교차표 검증과 별도 범위다.
- 65세 미만·이상 요약238행도 별도 원본 재합산 결과 전 셀·비중 차이0이며 지역 총계와16구군 합계가 일치했다. 증거: `data/interim/전체연령조건검증/independent-65plus-verification.json`. 기존 전체11개 연령구간은 보존했다.

- 7년 원본의 같은 행에서 기초 교차집계 CSV7개를 생성했다. 전 연도 원본 SHA256 및 전체·부산·처리결과·종별·월·시간·구군 합계를 기존 감사와 대조했다.
- 별도 pandas 파서로2024 원본866,835행을 다시 읽어7개 집계표의2024 모든 셀을 대조했으며 차이0이었다. 다른6년은 연도 합계 대조와 코드검토를 수행했다. 이 검증범위를7년 모든 교차셀 독립 재산출로 과장하지 않는다. 증거: `data/interim/119기초분석검증/independent-verification.json`.
- 처리조건 지역 비교 보고서의16구군+미기재17행 및 처리결과7행을 별도 검토자가 CSV와 대조해 일치를 확인했다. 정상 처리 안에도 업무운행이 남아 있음을 명시했다.
- 신규 기초집계와 보고서 반영 후 하네스10항목·`git diff --check` 통과. 임시 미구현 분기나 생략 테스트를 도입하지 않았다.

- CSV 스트리밍 파서의 청크·인용부호·줄바꿈 검사와 집계 불변식 검사를 통과했다.
- `git diff --check` 통과.
- 하네스는 이전 관광 폴더 강제 규칙을 제거하고 공통 구조를 검사하도록 수정했다. 실행 인터프리터를 재사용하고 Git 한국어 출력의 UTF-8 디코딩을 명시했다.
- 기존 `.venv`는 다른 사용자 PC의 Python 경로를 참조해 실행되지 않는다. 독립 검산은 Codex 제공 Python 3.12 환경을 명시적으로 사용했으며 기존 가상환경을 변경하지 않았다.
- 프로젝트 안에 검증용 `.venv-check`를 만들고 matplotlib을 설치했다. 기존 `.venv`와 Codex 제공 환경은 변경하지 않았다. `.venv-check`는 Git 제외 상태다.
- 공통 스타일이 Windows 맑은 고딕을 찾도록 수정했다. 실제 선택 폰트는 Malgun Gothic이며 부산·신고·연령 등의 한글 글리프가 존재함을 독립 확인했다.
- 새 검증 환경에서 하네스 10개 항목 모두 통과했다. 독립 재검증에서도 종료코드 0·경고 0이었다.

PowerShell 재현 명령:

```powershell
$env:PYTHONUTF8='1'
.venv-check/Scripts/python.exe scripts/check_harness.py
```

## 인력 제안 범위 정정

노인일자리 인력을 순찰에 투입하는 방안은 사용자 지시에 따라 현재 기획에서 제외했다. 노인일자리·노인복지관 자료는 신고 대상자의 연령을 보완하지 못한다. 노인일자리 자료는 현재 분석에서 사용하지 않으며, 복지관 자료도 현재 핵심 분석에서 사용하지 않는다. 대상 연령·유형·지역이 함께 있는 부산 원본 또는 공식 교차통계를 추가 탐색한다.

이번 변경은 로컬 develop 작업 트리에 있으며 커밋·푸시는 하지 않았다. 원본 데이터는 Git 제외 상태다.
