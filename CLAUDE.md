# 2026 Big Data 활용 대회 — Track 1 출품 프로젝트

부산시 공공데이터로 **지역 현안을 분석하고 정책·서비스 개선안을 시각화하여 제안**하는
공모전 출품작을 만든다. 이 파일은 매 세션 자동 로드되는 프로젝트 헌법이다.

---

## 1. 대회 팩트시트 (확정 사실만)

> 출처: https://www.allforyoung.com/posts/83834 (2026-08-19 확인). 상세는 `docs/00-공모요강.md`

| 항목 | 내용 |
|---|---|
| 대회명 | 2026 Big Data 활용 대회 |
| 주최/주관 | 부산광역시, 부산테크노파크 |
| 후원 | **경남연구원, 부산외국어대학교** |
| 운영 | DX Challenge |
| 우리 트랙 | **Track 1 — 빅데이터 분석 및 시각화** |
| 참가 형태 | **팀 (2~4인)** |
| 총 상금 | 650만원 (부산광역시장상 포함) |
| **제출물** | **보고서 10P 이하** (표지 제외) · 한글 서식 · 원본 `refs/2026-공모-작성서식.hwpx` |
| **추가 가점** | **데이터 오픈랩 방문 · Big-데이터웨이브(브라이텍스) 사용** — 서식 붙임 4 |
| 신청 | https://forms.gle/FQkrZAoodxiqHk388 (구글 로그인 필요) |
| 공고 | https://www.allforyoung.com/posts/83834 |

### 일정 — 절대 날짜
| 날짜 | 이벤트 |
|---|---|
| **2026-09-18 (금) 17:00** | **접수 마감 (하드 데드라인)** |
| 2026-09-23 (수) | 서류평가 결과 발표 |
| 2026-09-23 ~ 10-29 | 데이터 분석 및 발표 준비 (약 5주) |
| 2026-10-29 (목) | 발표평가 (최종) |

> 프로젝트 시작일 2026-08-19 기준 **접수까지 D-30**.
> D-day를 물으면 `date` 명령으로 오늘을 확인한 뒤 2026-09-18과의 차이를 계산할 것. 추측 금지.

### 심사 구조가 작업에 주는 함의
**2단계 대회다. 지금 만드는 건 "완성된 분석"이 아니라 "1차를 통과할 제안"이다.**

- 접수(9/18) → 결과(9/23)까지 **5일**. 심사자가 짧은 시간에 대량의 서류를 훑는다는 뜻.
  → 첫 페이지에서 "무슨 문제를, 무슨 데이터로, 어떻게 보여줄지"가 즉시 읽혀야 한다.
- 본 분석 기간 5주는 **통과 후에** 주어진다.
  → 9/18까지는 완결된 분석보다 **문제 정의의 날카로움 + 데이터 확보 증명 + 결과물 미리보기**가 중요.
- 최종은 **발표평가**다. 글이 아니라 화면으로 이긴다.
  → 처음부터 "발표 슬라이드에 들어갈 그림"을 목표로 분석을 설계한다.
- **보고서는 10P 이하다.** 6개 절을 다 채워야 해서 글을 늘릴 공간이 없다.
  **그림 한 장이 문단 셋을 대체하도록** 설계한다.

---

## 2. 실행 기준

1. **현행 주제는 `docs/30-아이디어/신고데이터-정책제안-실행계획.md`를 따른다.** 주제는 사용자 결정으로 확정했다. 데이터 실사 통과와 분석 완료를 의미하지 않는다. 기존 관광·예방지원·예측 문서는 검토 이력이다.
2. **Python은 가상환경 실행 파일 경로로 호출한다.** macOS/Linux는 `.venv/bin/python`, Windows는 `.venv/Scripts/python.exe`. 최초 환경 생성만 시스템 Python을 사용할 수 있다. activate 상태에 의존하지 않는다.
3. **원본은 불변이다.** `data/raw/`에 새 원본을 저장하되 기존 원본을 편집·덮어쓰기하지 않는다. 가공은 `data/interim/` → `data/processed/`로 한다.
4. 실제 데이터를 처음 확보할 때 `docs/20-데이터카탈로그/INDEX.md`를 만든다. 후보 목록은 실행계획에 두고, 카탈로그에는 실제 파일·출처·기간·공간단위·컬럼·행 수·검증·라이선스·원본 해시를 기록한다. 미등재 데이터로 분석하지 않는다.
5. **두 축을 구분한다.** 축 1은 신고 위경도·발생일시로 시공간 특성을 분석하고 동별 연령 구성과 비교한다. 축 2는 상권의 업종·규모·시점과 신고 특성을 비교한다. 결합 키는 행정동 코드와 기준기간이다.
6. **동별 연령은 신고 당사자의 나이가 아니다.** 인구비율로 연령별 신고 건수를 만들지 않는다. 상권 인접 신고를 해당 점포 사고로 간주하지 않는다. 신고·출동·확인 사고를 구분하고 지역 상관을 개인 위험·인과효과로 표현하지 않는다.
7. 부산 전역을 같은 기준으로 비교한다. 원본 좌표를 보존하되 주 비교 단위는 행정동이다. 법정동·경찰서 관할·상권 경계와 혼동하지 않는다. 일부 지역·일부 기간만 있으면 실제 범위와 보류한 분석을 표시한다.
8. 시간·계절은 발생일시로 계산한다. 좌표로 시간성을 추정하지 않는다. 누락 기간·관측일 수·계절 정의를 기록하고 짧은 기간의 차이를 반복적 계절성으로 단정하지 않는다.
9. 출처 없는 통계·합성 사건·임의 좌표로 산출물을 만들지 않는다. 모르는 값은 미확인, 원본 미확보 항목은 미검증이다. 데이터 실사 실패 시 해당 축을 보류하거나 범위 조정을 제안하고 과거 주제로 자동 전환하지 않는다.
10. 탐색은 `notebooks/`, 최종 그림·웹 집계는 재현 가능한 `analysis/` 스크립트로 만든다. 차트는 `analysis/style.py`의 `setup()`을 호출한다. 문서·주석·라벨은 한국어이며 한글 폰트와 최종 크기를 확인한다.
11. 정책은 관측 결과 → 활동 → 담당 업무 → 집행 조건 → 평가로 연결한다. 인력 배치·동선·미래 예측은 필수가 아니다. 기존 지원 자료 없이 공백, 실제 평가 없이 신고 감소 효과를 확정하지 않는다.
12. 보고서는 공식 서식 `refs/2026-공모-작성서식.hwpx`의 목차와 분량을 따른다. 대회 미확인 사항은 `docs/00-공모요강.md`의 작업 가정으로 관리한다.
13. 브랜치·커밋·PR은 `CONTRIBUTING.md`를 따른다. `main` 직접 push와 공유 브랜치 강제 push는 금지한다. 커밋·PR 전 가상환경 Python으로 `scripts/check_harness.py`를 실행하고 결함을 해결한다.
14. 사건별 좌표·원시 신고·개인정보는 공개 Git·웹에 올리지 않는다. 원본과 중간자료는 Git 제외이며 최종 집계도 공개 조건·소수 건수·개인 식별 가능성을 검토한다.

## 3. 디렉토리 지도

| 경로 | 역할 |
|---|---|
| `docs/30-아이디어/신고데이터-정책제안-실행계획.md` | 현행 주제·두 축·데이터 명세·판단 기준의 기준 문서 |
| `docs/30-아이디어/SHORTLIST.md` | 현행 안내와 과거 후보 이력 |
| `docs/20-데이터카탈로그/` | 실제 원본을 처음 확보할 때 INDEX.md 생성 |
| `docs/40-기획서/` | 공식 서식에 맞춘 제출 보고서 |
| `data/raw/` · `data/interim/` | 00_경계 · 01_신고 · 02_연령인구 · 03_상권 · 04_정책자원 |
| `data/processed/` | 검증된 분석용 집계·결합표 |
| `analysis/` · `notebooks/` · `figures/` | 00_공통 · 01_신고시공간 · 02_연령연계 · 03_상권연계 · 04_정책제안 · 05_검증 |
| `web/data/` | 공개 가능한 사전 계산 집계·GeoJSON |
| `refs/` | 제출 서식·참고 문헌·정책 근거 |

수집 대상 번호와 분석 단계 번호는 서로 대응하지 않는다. `analysis/README.md`에 단계별 입력·출력을 설명한다.

## 4. 작업 도구

- `/standup`: 현행 계획·실물 파일·카탈로그를 대조해 진척을 보고한다.
- `/datacheck`: 신고 → 경계 → 연령인구·상권 → 정책자원 순서로 실사하고 축별 가능 여부를 판단한다.
- `/idea`: 사용자가 요청한 범위 개선이나 데이터 제약에 따른 대안을 제안한다. 현행 주제를 임의로 교체하지 않는다.
- `/review`: 데이터·정책·시각화 관점에서 기획과 실제 근거를 검토한다.
- 스킬: `busan-data` (자료 확보·검증), `viz-style` (시각화), `proposal` (보고서).
- 하네스 검사: `.venv/bin/python scripts/check_harness.py` 또는 Windows의 `.venv/Scripts/python.exe scripts/check_harness.py`.

## 5. 현재 상태

> 2026-09-13 갱신

- 주제: **부산 신고 데이터를 활용한 지역 맞춤 정책제안**.
- 축 1: 신고 시공간 특성 × 동별 연령 구성. 축 2: 상권 특성 × 신고 특성.
- 주제 확정 / 데이터 실사 전. 이번 하네스 변경에서 원본을 확보하거나 분석하지 않았다.
- 다음 작업: 신고 원본의 위경도·발생일시·유형과 부산 전역·기간 포함 여부를 확인한다.
- 자료 확보 상태는 실제 `data/raw/`와 카탈로그를 함께 확인한다. 카탈로그 부재만으로 원본 0건을 단정하지 않는다.
