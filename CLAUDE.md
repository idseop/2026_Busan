# 2026 Big Data 활용 대회 — Track 1 출품 프로젝트

부산시 공공데이터로 **지역 현안을 분석하고 정책·서비스 개선안을 시각화하여 제안**하는
공모전 출품작을 만든다. 이 파일은 매 세션 자동 로드되는 프로젝트 헌법이다.

---

## 1. 대회 팩트시트 (확정 사실만)

> 출처: https://www.allforyoung.com/posts/83834 (2026-08-19 확인). 상세는 `docs/00-공모요강.md`

| 항목 | 내용 |
|---|---|
| 대회명 | 2026 Big Data 활용 대회 |
| 주최/주관 | 부산광역시, 부산테크노파크 |
| 후원 | **경남연구원, 부산외국어대학교** |
| 운영 | DX Challenge |
| 우리 트랙 | **Track 1 — 빅데이터 분석 및 시각화** |
| 참가 형태 | **팀 (2~4인)** |
| 총 상금 | 650만원 (부산광역시장상 포함) |
| **제출물** | **보고서 10P 이하** (표지 제외) · 한글 서식 · 원본 `refs/2026-공모-작성서식.hwpx` |
| **추가 가점** | **데이터 오픈랩 방문 · Big-데이터웨이브(브라이텍스) 사용** — 서식 붙임 4 |
| 신청 | https://forms.gle/FQkrZAoodxiqHk388 (구글 로그인 필요) |
| 공고 | https://www.allforyoung.com/posts/83834 |

### 일정 — 절대 날짜
| 날짜 | 이벤트 |
|---|---|
| **2026-09-18 (금) 17:00** | **접수 마감 (하드 데드라인)** |
| 2026-09-23 (수) | 서류평가 결과 발표 |
| 2026-09-23 ~ 10-29 | 데이터 분석 및 발표 준비 (약 5주) |
| 2026-10-29 (목) | 발표평가 (최종) |

> 프로젝트 시작일 2026-08-19 기준 **접수까지 D-30**.
> D-day를 물으면 `date` 명령으로 오늘을 확인한 뒤 2026-09-18과의 차이를 계산할 것. 추측 금지.

### 심사 구조가 작업에 주는 함의
**2단계 대회다. 지금 만드는 건 "완성된 분석"이 아니라 "1차를 통과할 제안"이다.**

- 접수(9/18) → 결과(9/23)까지 **5일**. 심사자가 짧은 시간에 대량의 서류를 훑는다는 뜻.
  → 첫 페이지에서 "무슨 문제를, 무슨 데이터로, 어떻게 보여줄지"가 즉시 읽혀야 한다.
- 본 분석 기간 5주는 **통과 후에** 주어진다.
  → 9/18까지는 완결된 분석보다 **문제 정의의 날카로움 + 데이터 확보 증명 + 결과물 미리보기**가 중요.
- 최종은 **발표평가**다. 글이 아니라 화면으로 이긴다.
  → 처음부터 "발표 슬라이드에 들어갈 그림"을 목표로 분석을 설계한다.
- **보고서는 10P 이하다.** 6개 절을 다 채워야 해서 글을 늘릴 공간이 없다.
  **그림 한 장이 문단 셋을 대체하도록** 설계한다.

---

## 2. 하드 룰 — 예외 없음

1. **Python은 반드시 `.venv/bin/python` 절대·상대 경로로 호출한다.**
   Bash 도구는 셸 상태를 유지하지 않으므로 `conda activate`·`source .venv/bin/activate`는 다음 호출에서 사라진다.
   `python`, `python3` 맨몸 호출 금지. → `.venv/bin/python script.py`
2. **`data/raw/`는 읽기 전용이다.** 내려받은 원본을 절대 수정·덮어쓰기 하지 않는다.
   가공물은 `data/interim/` (중간) → `data/processed/` (분석 투입용)으로만 쓴다.
3. **모든 데이터셋은 사용 전에 `docs/20-데이터카탈로그/INDEX.md`에 등재한다.**
   이 파일은 **첫 데이터를 받는 시점에 만든다** (미리 빈 표를 두지 않는다).
   등재되지 않은 데이터로 만든 분석·주장은 무효로 간주하고 되돌린다.
4. **차트를 그리는 모든 스크립트는 `analysis/style.py`의 `setup()`을 먼저 호출한다.**
   한글 폰트가 깨진 그림은 산출물이 아니다.
   **탐색은 `notebooks/`에서 하되, 최종 산출물(`figures/`·`web/data/`)은 반드시
   `analysis/` 스크립트가 만든다.** 노트북 출력에만 있는 그림은 재현이 안 되므로 산출물이 아니다.
5. **출처 없는 수치를 쓰지 않는다.** 문서의 모든 통계에는 `[출처: 기관명, 연도, URL]`을 붙인다.
   기억이나 추정으로 숫자를 만들어내지 않는다. 모르면 "미확인"이라고 쓴다.
6. **꾸며낸 데이터로 산출물을 만들지 않는다.**
   파이프라인 검증이 필요하면 `A구`·`값1` 같은 **명백한 더미**를 쓰고, 커밋하지 않는다.
   실제 지명에 그럴듯한 수치를 붙인 그림은 공개 저장소에서 실제 분석으로 읽힌다.
   `scripts/check_harness.py` 가 "데이터 0건인데 그림 존재"를 결함으로 잡는다.
7. **데이터 실사 없이 아이디어를 확정하지 않는다.**
   "좋은 아이디어인데 데이터가 없다"가 이 프로젝트 1순위 실패 원인이다.
   실사 = 실제로 내려받아 행 수·기간·컬럼을 눈으로 확인하는 것. 목록에 있다는 것만으로는 불충분.
8. **문서·코드 주석·차트 라벨은 전부 한국어.** 제출물이 한국어다.
9. **팀 산출물이다.** 문서는 팀원이 배경지식 없이 읽어도 이해되게 쓴다.
   결론을 맨 위에, 근거를 아래에.
10. **불확실한 요강 사항은 추측으로 채우지 말고 `docs/00-공모요강.md`의 "작업 가정" 표를 따른다.**
   미확인 항목은 **가정을 세우고 진행**하며, 가정은 틀렸을 때 손실이 적은 쪽으로 잡는다.
   새 정보가 나오면 그 표를 갱신한다.
11. **보고서는 공식 서식의 목차를 그대로 따른다.** 임의로 목차를 만들지 않는다.
   `refs/2026-공모-작성서식.hwpx` 붙임 2가 사실상 배점표다. 상세는 `docs/00-공모요강.md`.
   **10P 이하 준수 필수.**
12. **협업 규칙은 `CONTRIBUTING.md`를 따른다.** 브랜치·커밋 메시지·PR·충돌 방지 규칙이
   거기 있다. 브랜치를 만들거나 커밋 메시지를 쓰기 전에 그 문서를 읽는다.
   - **`main`에 직접 push 금지.** 작업은 `develop`, main 진입은 Pull Request로만.
     로컬 pre-push 훅과 GitHub 브랜치 보호로 두 겹 차단돼 있다. 우회하지 않는다.
   - 커밋 전 `.venv/bin/python scripts/check_harness.py` 를 통과시킨다.
13. **모든 분석의 공간 입도는 행정동으로 통일한다.** ("다음 동네" 추천이 동 단위이므로 필수)
   레이어마다 입도가 다르면 지도가 합쳐지지 않는다. 원본이 더 잘면 행정동으로 집계하고,
   더 굵으면 그 레이어는 버리거나 질문을 바꾼다.
14. **취약성 지수의 가중치는 근거를 대거나 민감도 분석을 붙인다.**
   "왜 폭염 0.3, 침수 0.4인가"는 발표평가에서 반드시 나온다. 답이 없으면 지수 전체가 무너진다.
   서식이 **"분석결과 검증방법"**을 별도 항목으로 요구하는 것도 같은 맥락이다.
15. **오픈랩·데이터웨이브는 가점 항목이다. 기회가 되면 반드시 쓴다.**
   서식 붙임 4에 방문일시·사용데이터·브라이텍스 캡처를 적는 칸이 따로 있다.
   공개 데이터만으로 분석을 완결시키되, **가점 재료를 추가로 얹는다.**

---

## 3. 디렉토리 지도

```
CLAUDE.md                      ← 이 파일. 프로젝트 헌법
docs/
  00-공모요강.md                대회 요강 + 서식 확정 사항 (공고·서식 원문 근거)
  20-데이터카탈로그/            첫 데이터를 받을 때 INDEX.md 를 만든다
  30-아이디어/SHORTLIST.md       확정 주제
  40-기획서/                    보고서를 쓸 때 여기에 만든다 (목차는 proposal 스킬)

data/raw/                      원본 (읽기 전용, git 제외)
  00_경계/                      행정동·구 경계 SHP
  01_외국인/                     국적별 외국인·관광객 통계
  02_소비/                       카드·동백전 소비 (쏠림의 원천)
  03_방문/                       유동인구·관광지 방문
  04_숙박/                       숙박시설 (수용력)
  05_상권/                       업종·상가 (동네 프로파일)
  06_접근성/                     지하철·버스 (동네 프로파일)
data/interim/                  도메인별 중간 가공물 (git 제외, 폴더 구조는 유지)
data/processed/                분석 투입용 최종 테이블 (행정동 단위로 통일)

notebooks/                     탐색용 — 데이터 처음 열어보기, 실사, 시행착오
  README.md                    analysis/ 와의 역할 분담 + 첫 셀 보일러플레이트

analysis/                      재현 가능한 파이프라인 (최종 산출물은 여기서만 만든다)
  README.md                    번호 규칙 + 스크립트 작성 규약 (import 방식 포함)
  style.py                     차트 공통 스타일 (setup·save·choropleth)
  00_공통/                      경계 로드 · 행정동 기준 테이블 — 모든 단계의 기반
  01_쏠림진단/                   집중도(HHI 등)·시계열 → 문제 정의
  02_동네프로파일/                동별 특성 벡터 (상권·숙박·접근성·명소)
  03_다음동네/                   유사도 기반 "다음 동네" 추천 ★ 차별화 지점
  04_수용력/                     숙박·혼잡 한계 경보
  05_시뮬레이션/                  분산 시 구별 소비 재분배

figures/                       발표·기획서용 차트 (단계별, analysis 와 같은 번호)

web/                           최종 산출물 (정적 웹)
  index.html
  data/                        사전 계산된 GeoJSON
  assets/

refs/                          참고 자료, 타 지자체 사례, 정책 문서
```

> **번호 규칙**: `data/` 는 **수집 대상**, `analysis/`·`figures/`·`notebooks/` 는 **분석 단계**다.
> 둘은 1:1 대응이 아니다 — 여러 원본이 한 단계에 들어간다.
> 예: `01_쏠림진단`은 `01_외국인`+`02_소비`+`03_방문`을 함께 쓴다.

## 4. 도구

- `/standup` — 현재 위치, D-day, 다음 액션 브리핑
- `/idea` — 현안에서 아이디어 생성 + 채점 + 숏리스트 갱신
- `/datacheck` — 아이디어의 데이터 실현 가능성 실사 → 카탈로그 등재 → GO/조건부/NO 판정
- `/review` — 심사위원 관점으로 기획서를 깨는 비평

**하네스 자가 점검** — 구조가 깨졌는지 사람이 묻기 전에 확인한다:
```bash
.venv/bin/python scripts/check_harness.py
```
클론 재현성 · import 규약 · 꾸며낸 그림 · 출처 없는 수치 · 경로 참조 · 카탈로그 정합을 검사한다.
**커밋 전과 PR 전에 돌린다.**

스킬: `busan-data` (데이터 소스 지도·실사 절차) · `viz-style` (시각화 규약) · `proposal` (기획서 작성)

## 5. 현재 상태

> 2026-08-22 갱신 · 접수 마감 D-27

- **단계: 1 — 주제 선정 완료. 다음은 데이터 실사.**
- **메인 주제**:「4개 구가 75%」 **외국인 관광 쏠림 진단 + "다음 동네" 분산 추천기**
  - 제목의 수치는 **미검증**이다 — 근거는 `docs/30-아이디어/SHORTLIST.md` 참조
  - 쏠림 진단만으로는 기존 분석과 겹친다. **추천·시뮬레이션**이 차별화 지점
  - **오픈랩과 직결** — 서식 예시가 `동백전 사용내역 / 부산 빅데이터 오픈랩`이고
    소비 쏠림 분석에 바로 필요하다. 오픈랩 사용은 가점 항목 (하드 룰 15)
  - 국적별 분석을 붙이면 부산외대 계열 상 사정권
- **예비 주제**:「면허는 반납해도 갈 곳이 없다」 고령운전자 사고 위험 × 대중교통 사각지대
  - 메인이 실사에서 막히면 전환. 폴더 체계·시각화 도구는 그대로 재사용
- **아직 확정 아님.** 데이터 실사(하드 룰 7) 통과가 조건이다.
  최대 미확인은 **외국인 소비·방문 데이터의 공간 입도** — 구 단위까지만 나오면
  "다음 동네"(동 단위) 추천이 성립하지 않는다
- 데이터: **0건**. 아직 아무것도 내려받지 않았다
- 팀: 4인 (`idseop` `seongjae0709` `seungjoo-03` `hayun-0`)
